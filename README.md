# Hugging_face

- Install the lib in Bash
pip install "git+https://github.com/huggingface/diffusers.git"

Other dependencies
- pip install transformers accelerate torch torchvision sentencepiece protobuf







